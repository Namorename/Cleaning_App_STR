import { SuppliesView } from '@/features/supplies/supplies-view';

/** Stage 2: supply requests. The data is the client's to fetch. */
export default function SuppliesPage() {
  return <SuppliesView />;
}

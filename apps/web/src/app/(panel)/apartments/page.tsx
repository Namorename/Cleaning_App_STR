import { ApartmentsView } from '@/features/apartments/apartments-view';

/** Stage 5: the registry of listings. The data is the client's to fetch. */
export default function ApartmentsPage() {
  return <ApartmentsView />;
}

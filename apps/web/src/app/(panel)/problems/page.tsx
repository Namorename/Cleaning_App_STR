import { ProblemsView } from '@/features/problems/problems-view';

/** Stage 1: the board and the list. The data is the client's to fetch. */
export default function ProblemsPage() {
  return <ProblemsView />;
}

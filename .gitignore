# --- secrets ---
.env
.env.*
!.env.example
*.raw
# Приватные ключи — намеренно по всему дереву, а не только в apps/mobile:
# случайно закоммиченный apns-auth.key или service-account.key дороже,
# чем неудобство от тестовой фикстуры, которую придётся добавить через -f.
*.key
*.pem

# --- dependencies ---
node_modules/
.pnp
.pnp.js

# --- builds ---
dist/
build/
.next/
out/
*.tsbuildinfo

# --- expo ---
# Паттерны привязаны к apps/mobile: без ведущего слэша `ios/` и `*.key`
# срабатывают на любой глубине и молча выкидывают чужие файлы из коммита.
.expo/
.expo-shared/
/apps/mobile/ios/
/apps/mobile/android/
/apps/mobile/**/*.jks
/apps/mobile/**/*.p8
/apps/mobile/**/*.p12
/apps/mobile/**/*.keystore
/apps/mobile/**/*.mobileprovision

# --- supabase ---
supabase/.branches/
supabase/.temp/
supabase/.env

# --- tooling ---
.turbo/
coverage/
*.log
npm-debug.log*

# --- os / editors ---
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/*
!.vscode/extensions.json

# --- vendor docs (крупные бинарники держим локально) ---
docs/vendor/*
!docs/vendor/README.md


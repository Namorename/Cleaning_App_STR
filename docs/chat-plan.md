# F29. Чат менеджера и исполнителя — план и чек-лист

> **Слой 5 приостановлен 2026-09-19 до запуска с горничными.** Готово всё,
> кроме выката окна 2: три миграции (`20260918180000`, `20260918190000`,
> `20260918200000`) лежат в репозитории **невыкаченными**, Vercel и OTA не
> трогаем. За выкат не браться — только по слову владельца.

Менеджер сегодня говорит горничной то, что надо сказать про конкретную уборку,
тремя способами, и ни один из них не двусторонний: заметка объекта
(`properties.cleaner_notes`, постоянная), заметка задания (`tasks.notes`,
которая при старте становится шагом `task_note` и требует протыкать каждую
строку) и WhatsApp мимо системы. Ответить горничная может только четвёртым —
позвонить. Репорт о поломке идёт от неё к технику через две таблицы
(`problems` → `tasks.problem_id`) и ни одной строки текста между людьми.

Это план разговора: ветка у предмета (задание, поломка), личная ветка у
сотрудника, фото через тот же конвейер, что и всё остальное. Порядок этапов
утверждён 2026-09-17. Каждый этап — отдельный коммит с тестами; выкат по
правилу «миграции и сборка вместе».

## Решения

- **Ветка принадлежит предмету, а у связки «поломка + ремонт» предмет один —
  поломка.** Задача типа `maintenance` с `problem_id` рисует ветку своей
  поломки, а не заводит свою: отменённая попытка ремонта
  (`tasks_one_fix_per_problem` её разрешает) не уносит разговор с собой, и
  второй техник приходит в ту же переписку. Подмена адреса живёт **в одной
  серверной функции `open_thread`**, не в клиентском `if type ===
  'maintenance'`: ручной `maintenance` без `problem_id` свою ветку иметь
  обязан, а правило, переписанное в панели, в телефоне и в будущем
  нотификаторе, — это находка 6.1 слово в слово.
- **Ветка уборки, на которой нашли поломку, остаётся отдельной.** У них разная
  аудитория и разный предмет. Связь — системная отбивка «заведена поломка» со
  ссылкой; хранения она не добавляет.
- **Кто читает предмет — тот читает и разговор о нём.** Аудитория ветки
  задания — аудитория задания (`assignee_id` **или** `cleans_property`),
  аудитория ветки поломки — аудитория поломки, аудитория личной — сотрудник и
  менеджеры. Отвергнуто «только исполнитель»: оно ломает подмену коллеги, ради
  которой политика задач шире назначения, и убивает главный случай — **на
  свободном задании написанное менеджером заранее читать было бы некому**,
  пока кто-то не возьмёт. Отвергнут и свой список участников на ветке: он
  немедленно разошёлся бы с горизонтом, архивом, деактивацией и тенантом.
- **Доступ следует за предметом, а не за авторством.** Техник, с которого сняли
  ремонт, теряет ветку вместе с поломкой — включая собственные сообщения.
  Политики «автор всегда читает своё» не будет: она оставила бы уволенному
  доступ к фотографии чужой квартиры.
- **Чат ничего не заменяет; заметка — норматив, чат — разговор.**
  `tasks.notes` через `snapshot_task_steps()` становится шагом `task_note`, где
  каждую непустую строку надо отметить, и гейт завершения это проверяет. Чат
  отметить нельзя. Одной фразой: **в заметке то, что надо сделать; в чате то,
  что надо обсудить.**
- **Заметка в ветке не закрепляется; развилка стоит в момент письма.** Шаг
  показывает **снимок** заметки — он для того и снимается, чтобы правка
  посреди уборки не перенумеровала протыканное; закреп показывал бы **живую
  колонку**, и через минуту после старта это два разных текста одной
  инструкции, причём обязателен тот, которого в закрепе не видно. Вместо
  закрепа — переключатель «это инструкция» у окна ввода в панели: он пишет
  `tasks.notes` существующим путём сохранения задания, а не сообщение. Одно
  окно, два адресата, выбирает автор.
- **Личная ветка — ящик компании, а не переписка с человеком.** Ровно одна
  строка на `(host_id, profile_id)`; собеседник сотрудника — «офис», читают и
  пишут все менеджеры и админы хоста. Отпуск, смена и мультитенантность
  решаются без таблицы участников и без адресной книги; кто именно ответил,
  видно по автору сообщения. Переписка менеджер↔менеджер и горничная↔горничная
  **вне объёма**: у горничной нет и не будет списка сотрудников, а всё, у чего
  есть предмет, живёт в ветке предмета.
- **Имя и роль автора лежат на сообщении, а не берутся из `profiles`.**
  Политики на `profiles` дают горничной ровно свою строку, поэтому подписать
  чужое сообщение телефону нечем. Заодно это стенограмма: подпись — то, кем
  человек был в тот момент; `author_id` уходит в `null` по
  `on delete set null`, как `task_media.created_by`.
- **Предикат участия выписан явно, а не делегирован политикам предмета.**
  `security invoker`-функция, вызванная изнутри `security definer`,
  выполняется от имени definer'а и RLS **не применяет** — внутри
  `send_message`, которая обязана быть definer, приём «пусть ответят политики
  задачи» вернул бы `true` на всё. Приём `can_read_task_media` держится только
  потому, что его зовёт **политика**, а она считается в контексте вызывающего.
  Поэтому правило живёт в `chat_participates`, и её же зовут политики —
  разойтись им негде.
- **Фото сообщения — третий владелец в том же конвейере.**
  `task_media.message_id`, XOR расширяется с двух владельцев до трёх — ровно
  тем движением, каким `20260908130100_problem_media.sql` добавил второго. Путь
  `<host_id>/chat/<thread_id>/<media_id>.<ext>`: тред и есть единица ретенции и
  единственная осмысленная папка для оператора. Отвергнута отдельная таблица —
  `CLAUDE.md`: «новый вид медиа — новый владелец в том же конвейере».
- **Видео в чате нет.** Шаг просит видео, потому что шаг объявил
  `max_video_sec`; в чате объявлять некому, а 150 МБ на мобильном интернете —
  это счёт. Только фото, до четырёх, тем же лимитом, что у репорта.
- **Непрочитанное — маркер на читателя, не флаг на сообщении.** У ветки задания
  несколько читателей, и перечислить их нельзя в принципе: аудитория выводится
  из RLS предмета, а не хранится. `chat_reads (thread_id, profile_id,
  last_read_at)`, первичный ключ — пара.
- **Отправка — это прочтение.** `send_message` двигает собственный маркер
  автора вперёд. Формула «новее маркера и автор не я» ошибается на
  последовательности «я → она → я»: последний автор снова я, и её сообщение
  теряется. С подвижкой маркера непрочитанность становится чистым сравнением
  двух дат.
- **Открытие ветки помечает прочитанным нарисованное, а не всё.**
  `mark_thread_read(p_thread_id, p_up_to)` двигает маркер к
  `greatest(старое, p_up_to)` — монотонно, поэтому запоздавший повтор никогда
  не «разчитывает».
- **Офлайн — существующая очередь мутаций, без своей.** Клиентский id,
  идемпотентный `send_message(p_id, …)`, шестой регистратор
  `registerChatMutations` **до** восстановления кэша. Порядок сообщений держит
  свой scope `{ id: 'chat-send' }`; фото уезжают отдельными мутациями в
  существующую очередь `['media','attach']`, иначе текстовое сообщение встанет
  за чужим двадцатимегабайтным видео.
- **`buster` не трогаем, и это решение.** Смена `buster` выбрасывает весь
  персистентный кэш **вместе с приостановленными мутациями** — обновление ради
  чата стёрло бы горничной очередь «взял/начал/завершил», набранную в подъезде.
  Поэтому непрочитанное живёт отдельным ключом запроса, а не колонкой в строке
  задания: форма `tasks` не меняется и `tasks-v6` остаётся в силе.
- **Ни Realtime, ни общего опроса.** Опрос — только пока экран ветки открыт и
  приложение на переднем плане; счётчики обновляются на `AppState` → active, на
  pull-to-refresh и после своей отправки. Подписка на каждый телефон в поле
  стоит соединения круглые сутки ради нескольких сообщений в день. Но опрос —
  **не затычка вместо push**: push будит закрытое приложение, опрос обслуживает
  открытый экран, и после F11 нужны оба.
- **Чат выкатывается до F11, осознанно.** Ветка задания без push работает:
  горничная обязана открыть приложение, чтобы стартовать задачу, и сообщение
  ждёт там, где она всё равно смотрит. Личная ветка без push слабее, поэтому
  она и стоит в слое F11 — и её микрокопия обязана не врать: «увидит при
  следующем открытии», а не «отправлено». Решающий довод за такой порядок —
  push это `plugins`/`permissions` в `app.json`, то есть новая сборка EAS и
  подъём `version`; чат — чистый JavaScript и уезжает по воздуху.

## Модель данных

Три таблицы, один enum, вид и четыре RPC. Форма домашняя: `host_id` в каждой
таблице, предикат `host_id = public.current_host_id()` в каждой политике,
клиенту только `grant select`, запись — через RPC.

- `chat_threads` — `id`, `host_id`, `kind` (`task` / `problem` / `direct`),
  `task_id` / `problem_id` / `profile_id` под XOR-ом
  `chat_threads_one_subject`, `created_at`, и денормализованный триггером
  хвост: `last_message_at`, `last_author_id`, `message_count`. Три частичных
  уникальных индекса — по задаче, по поломке, по паре `(host_id, profile_id)`.
- `chat_messages` — `id` (**придумывает телефон**, как `problems.id`),
  `thread_id`, `host_id`, `author_id` (`on delete set null`), `author_name`,
  `author_role`, `body`, `media_expected`, `created_at`. Проверка
  `length(btrim(body)) > 0 or media_expected > 0`: строка пишется первой, файлы
  идут следом, и `media_expected` — **заявление**, как `task_media.source`;
  без него пустое сообщение под словом «вот» было бы допустимо. С этапа 5 у
  колонки ровно одна работа — лицензия на фото (разрешает пустой текст и
  ограничивает регистрацию); сколько фото в сообщении, говорят только живые
  строки `task_media`, отрисовка заявление не читает.
- `chat_reads` — `(thread_id, profile_id)` первичным ключом, `host_id`,
  `last_read_at`, `updated_at`.
- `chat_participates(kind, task_id, problem_id, profile_id)` — единственное
  место, где написано правило доступа; `security definer`, предикат выписан.
  `is_manager()` первым в каждой ветке, чтобы `cleans_property` и
  `task_is_beyond_horizon` у менеджера не вычислялись.
- `open_thread`, `send_message`, `mark_thread_read` и `chat_unread_threads`
  (definer-RPC; вид `chat_unread` с `security_invoker` планировался и
  отвергнут по замеру слоя 4 — см. чек-лист).
- Слой 5: `task_media.message_id`, трёхсторонний XOR, `add_message_media`,
  политика чтения, ветки в `remove_task_media` и `task_media_to_purge`.

Все четыре новые сущности вписываются в матрицу
`supabase/tests/table_grants.sql` **той же миграцией** — набор падает на
таблице, которой в матрице нет, и это сделано нарочно.

## Этапы

| # | Этап | Что делает | Объём | База |
| --- | --- | --- | --- | --- |
| 1 | Схема и правила | enum, три таблицы, `chat_participates`, `open_thread`, `send_message`, `mark_thread_read`, триггер хвоста, политики, гранты, строки в матрице; набор `supabase/tests/chat.sql` | M | 1 миграция |
| 2 | Менеджер пишет | панель: `features/chat`, ветка в задании и в карточке поломки | M | — |
| 3 | Исполнитель читает и отвечает | телефон: экран ветки, блоки на задании и поломке, шестой регистратор мутаций, опрос при открытом экране | M | — |
| 4 | Непрочитанное | вид `chat_unread`; метка на карточке задания и поломки, `focusManager` + `AppState`, счётчик в меню панели | M | 1 миграция |
| 5 | Фото в сообщении | третий владелец `task_media`, `add_message_media`, ретенция; вложения на обоих клиентах | M | 1 миграция |
| 6 | Личная ветка и пробуждение | ящик компании на обоих концах; push на новое сообщение — делается **вместе с F11** | M | — |
| 7 | Летопись и отбивки | системные строки в ветке о том, что происходило с предметом: взято, начато, завершено, отменено, истекло, переназначено, «заведена поломка» (со временем и кем); переключатель «это инструкция» | M | 1 миграция |

Порядок неслучаен. Этап 2 идёт **перед** телефоном: главный сценарий владельца
начинается с менеджера, и ветка, в которую некому написать, — не выкат. Этап 6
делается на обоих концах одним куском: личная ветка, которую никто не читает,
хуже отсутствующей. Этапы 4, 5 и 7 самостоятельны и переставляются.

## Чек-лист

- [x] 1. Схема, правила доступа и RPC — `20260918120000_chat.sql`, набор
      `chat.sql` (44 проверки). Предполёт: 100 агентов, семь линз, 24
      подтверждённые находки, семь исправлены до коммита. Главное допущение
      доказано экспериментом: `security invoker` внутри `security definer`
      выполняется от владельца (`postgres`, `BYPASSRLS`) и RLS не применяет —
      делегирование предиката политикам задач отвечало бы «да» на всё, включая
      чужую компанию; политика же вычисляется в контексте вызывающего.
      Исправлено: индекс `desc nulls last` вместо `desc` (57x на 1500 нитях,
      потому что `desc` значит `nulls first`); вызывающий читается один раз
      вместо четырёх `definer`-вызовов на строку (было 0.8 мс у менеджера и 3.9
      у горничной); `coalesce(…, false)` против прохода по `null` сквозь оба
      гейта; горизонт в ветке поломки; ящик компании переживает увольнение;
      прямая нить только для полевого персонала; `open_thread` не возвращает
      строку из нулей; повтор id из чужого тенанта отвечает ключом. Попутно
      закрыта дыра F9 — `20260918110000_fixer_audience.sql`, отдельным
      коммитом.
- [x] 2. Менеджер пишет в ветку задания и поломки — `apps/web/src/features/chat/`
      в домашней шестифайловой форме: `open_thread` при открытии карточки,
      чтение `chat_messages` напрямую, `send_message` с id, придуманным до
      отправки (повтор после отказа переигрывает тот же id), `mark_thread_read`
      до самого нового нарисованного сообщения. Ветка встроена в drawer
      задания и в карточку поломки; drawer теперь открывается у **любого**
      задания (кнопка «Разговор» у незавершённого, «Как прошла уборка» у
      завершённого) — главный случай функции, записка на невзятом задании,
      иначе не имел бы экрана; разделы о работе показываются только начатому,
      корректировка времени — только завершённому. Опрос раз в 15 с, пока
      ветка на экране и вкладка впереди. Под заголовком написано, кто увидит
      сообщение: у невзятого задания это исполнитель, горничные объекта и
      менеджеры. Прочтения менеджеру пока не показываются — слой 4.
- [x] 3. Исполнитель читает и отвечает с телефона — `apps/mobile/src/features/chat/`
      и экран `app/chat/[subject]/[id]`; кнопка «Чат» на экране задания (у любого
      видимого задания, не только своего — записка на невзятом задании читается
      из очереди) и на экране поломки. Шестой регистратор `registerChatMutations`
      стоит до восстановления кэша; отправка идёт в своём scope `chat-send` с id,
      придуманным на телефоне, и **видна сразу** серым пузырём «Отправляется…»
      из `useMutationState` — переживает подъезд без связи и повтор после
      перезапуска. Опрос раз в 10 с только при фокусе экрана (`useFocusEffect`)
      и активном приложении: `lib/app-focus.ts` впервые связал `AppState` с
      `focusManager` TanStack — до этого клиент считал приложение всегда
      на переднем плане, и любой опрос шёл бы за заблокированным экраном.
      Побочный эффект намеренный: устаревшие списки обновляются при
      возвращении в приложение. Список перевёрнут (`inverted`), открывается на
      последнем сообщении без `scrollToEnd`. Нативных правок нет — уезжает по
      воздуху. **Живой прогон панель ↔ телефон проверен владельцем
      2026-09-18:** отправка, доставка, офлайн-очередь без дублей.
- [x] 4. Непрочитанное видно на карточке и в меню — `20260918160000_chat_unread.sql`.
      **Не вид, а definer-RPC `chat_unread_threads(p_task_ids, p_problem_ids)`,
      и это решение по замеру.** Предполёт слоя 4 на локальном стеке (1500
      нитей, 30 000 сообщений, 5000 маркеров, 25 горничных, 2000 задач):
      invoker-вид с построчной политикой отвечает менеджеру на «сколько
      непрочитанного» за 498 мс (политика `chat_threads` зовёт
      `chat_participates` на каждую строку, политика `chat_reads` — `is_manager()`
      на каждую совпавшую), горничной по всей компании — за 805 мс
      (`chat_participates` у полевого персонала стоит 0,5 мс на строку: задачи,
      горизонт, `property_cleaners`). Та же формула внутри definer-функции,
      которая читает вызывающего один раз и не спрашивает правило там, где
      ответ известен (менеджер читает всё в своей компании — первая ветка
      `chat_participates`), — 1,4 мс. Горничной функция задаётся **с id
      с экрана**: 60 задач — 20 мс, правило считается только для совпавших
      строк. Без id горничная получает правильный, но медленный ответ, и он
      дорожает с размером компании, а не с тем, что она видит, — слой 6 не
      должен так вешать бейдж на вкладку. Панель: одна выборка по компании,
      опрос раз в 60 с при вкладке впереди, бейдж «Новое сообщение» на
      карточке задания и поломки, счётчик нитей у «Заданий» и «Проблем» в меню;
      прочтение и отправка снимают метку. Телефон: `useUnreadSubjects(ids)` с id
      загруженного списка, обновление при возвращении в приложение
      (`focusManager`), по pull-to-refresh и после своего прочтения или
      отправки, без опроса; метка на карточке в «Моих», в очереди (записка на
      свободной работе — главный случай) и в поломках, слово попадает и в
      подпись для скринридера. Набор `chat.sql` +20 проверок, в том числе
      находка ревью: короткий путь менеджера действует только для веток
      задания и поломки, личная ветка всегда идёт через `chat_participates` —
      иначе ящик горничной, повышенной до менеджера, оставался бы виден офису
      через RPC, хотя политики его уже прячут. Предполёт слоя 4 (22 агента,
      `.claude/workflows/chat-layer4-preflight.js`): утечек нет — для девяти
      вызывающих из двух компаний множество функции совпало с множеством RLS в
      обе стороны; но `security definer` SQL-функция не инлайнится и планируется
      с массивами как неизвестными параметрами, а `or` с дизъюнктом только по
      параметрам не индексируется — вызов телефона с 60 id шёл Seq Scan по всей
      `chat_threads` всех тенантов (51 500 строк отброшено фильтром). Исправлено
      до выката: кандидаты в двух ветках `union all` (компания / id), под
      обобщённым планом ветка id даёт BitmapOr по двум частичным индексам, ~3 мс
      на 51 500 нитях; `me` объявлен `not materialized`, иначе маркер ищется
      хешем по всей `chat_reads`. Ветка компании (менеджер без id) осталась
      Seq Scan: при неизвестном хосте планировщик оценивает полтаблицы и
      индекс хоста не берёт даже со скалярным подзапросом (проверено) — 15–20 мс
      на 51 500 нитях, принято. Ремонт живёт в ветке поломки, поэтому телефон
      передаёт и `problem_id` техзадач с экрана, а карточка ремонта светится по
      поломке. Прочтения менеджеру («прочитано в 09:14») не рисуются — отложено
      до запроса.
- [ ] 5. Фото в сообщении. **Миграция готова** (`20260918180000_chat_media.sql`,
      2026-09-18): `task_media.message_id`, трёхсторонний XOR
      `task_media_one_owner`, частичный индекс по сообщению, политика чтения
      `"chat message media is read by whoever reads the message"`
      (через политики `chat_messages` → `chat_threads` →
      `chat_participates`), `add_message_media` по форме
      `add_problem_media`: только автор, только пока он участвует в ветке
      (доступ следует за предметом — снятый с ремонта техник не дополняет своё
      же сообщение), только фото, **не больше, чем сообщение заявило в
      `media_expected`** — заявление и есть лимит, текстовое сообщение фото не
      принимает; путь `<host_id>/chat/<thread_id>/<media_id>.<ext>`.
      Гейт галереи в чате **не применяется**: он про улику шага, а офис
      прикладывает из браузера, у которого камеры нет — при выключенном по
      умолчанию тумблере панель осталась бы без вложений; `source` всё равно
      записывается как заявление. `remove_task_media`: фото сообщения
      забирается только пока файл не подтверждён (загрузка не удалась);
      подтверждённое — часть сказанного, ключ `serverErrors.messageMediaSent`.
      `task_media_to_purge`: предмет ветки подставляется через `coalesce`, так
      что ветка задания и поломки стареет с их закрытием той же формулой, а
      личная — по возрасту сообщения. Набор `chat.sql` +25 проверок.
      **Решения перед клиентами** (2026-09-18, после разбора офлайна):
      - *Цепочка фото на телефоне* — одна воспроизводимая мутация в очереди
        `['media','attach']` на каждое фото: идемпотентный `send_message`
        (вернёт ту же строку) → `add_message_media` → загрузка на
        `storage_path` → `confirm_task_media`. Первый шаг нужен, потому что
        очереди `chat-send` и `media-attach` независимы: без него регистрация
        обогнала бы своё сообщение и получила «Message not found». Текст
        по-прежнему уходит своей быстрой очередью и не ждёт за чужим видео.
      - *Дырка по строке, не по заявлению.* Получатель рисует живые строки
        `task_media` сообщения: подтверждённая — фото, неподтверждённая
        (`uploaded_at is null`) — серая плитка «фото в пути»; строки, которой
        ещё нет, нет и на экране. `media_expected` для отрисовки **не читается
        никогда** — у колонки одна работа, *лицензия на фото*: она разрешает
        пустой текст (`body or media_expected`) и ограничивает, сколько строк
        можно завести под сообщение; число фото в сообщении — только строки
        `task_media`. Пузырь без слов и без живых строк рисует одну серую
        плитку по признаку «текст пуст», а не по числу.
      - *Офлайн отправителя.* Пауза сети мутацию не роняет (`offlineFirst`):
        плитки «ждёт сигнала» рисуются из локального файла, продолжение само
        при сигнале или при следующем запуске, три попытки; следующее фото
        ждёт за предыдущим в том же скоупе. Отказ сервера по существу — плитка
        «не удалось» с «повторить» и «убрать» (`remove_task_media` отдаёт
        неподтверждённую строку), как на экране шага.
      - *Срок неподтверждённой строки* — на сервере, в одном месте, а не
        порогом в двух клиентах: ветка `task_media_to_purge` «строка сообщения
        без файла старше `chat_media_upload_window()` (24 ч)», суточная метёлка
        (04:30) ставит `purged_at`. **Порог сутки + метёлка раз в сутки =
        фактически 24–48 часов.** Читатели `task_media` в обоих приложениях
        фильтруют `purged_at is null`. У отправителя исход один для всей
        цепочки: `add_message_media` и `confirm_task_media` по покойной строке
        отвечают `serverErrors.messageMediaExpired`; загрузку на покойный путь
        бакет и так не пускает (`can_upload_task_media` требует
        `purged_at is null`), и на отказ бакета цепочка переспрашивает
        регистрацию, чтобы показать именно «срок вышел», а не сырой текст
        Storage. Такая плитка — «не удалось» с единственным действием
        «убрать». Отдельная миграция `20260918200000_chat_media_expiry.sql`.
      - *Кто видит «срок вышел» — и чем он нарисован.* Фильтр `purged_at is
        null` на эмбеде **остаётся**, а «срок вышел» рисуется по возрасту
        сообщения: метёлка убирает строку из выборки, и запасная ветка
        «пустой пузырь» обязана замолчать, как только сообщение старше
        `chat_media_upload_window()`, — иначе фото без слов навсегда остаётся
        «фото в пути», ровно той плиткой, ради которой метёлка и написана
        (находка предполёта окна 2, подтверждена двумя линзами независимо).
        Вариант «снять фильтр `purged_at` и завести колонку в эмбед»
        **отвергнут**: он вернёт и фото, вычищенные ретеншном через 90 дней,
        — с рабочей плиткой и мёртвой ссылкой.
      - *Гонка идемпотентности.* `send_message`, `add_task_media`,
        `add_problem_media` и `add_message_media` ищут строку по id и вставляют
        без `on conflict`: два одновременных вызова с одним id дают
        проигравшему голый `23505` без ключа. Цепочка выше сама создаёт такую
        пару (`send_message` из двух очередей). Чинится одной формой во всех
        четырёх — `insert … on conflict (id) do nothing returning *`, при пустом
        результате перечитывание с той же проверкой хоста и автора —
        **отдельной миграцией и коммитом**, не внутри слоя чата:
        `add_task_media` и `add_problem_media` — живой путь фото уборок и
        откатываются независимо. Тест на параллельный вызов в одной
        транзакции: триггер-подстава вставляет ту же строку между поиском и
        вставкой RPC. **Два окна выката** (решение владельца 2026-09-18):
        окно 1 — `20260918170000_media_rpc_race.sql` (помощник
        `task_media_written_meanwhile(id, step, problem)`, `add_task_media`,
        `add_problem_media`) едет раньше чата и живёт день под одним
        пользователем; окно 2, после панели — `20260918180000_chat_media`,
        `20260918190000_chat_rpc_race` (та же форма в `send_message` и
        `add_message_media`, проверка владельца-сообщения выписана в самой
        функции, помощник чата не знает) и `20260918200000_chat_media_expiry`.
        Метёлка в окно 1 не входит: без сообщений ей нечего мести.
        **Предполёт окна 1** (2026-09-19, 25 агентов,
        `.claude/workflows/media-rpc-race-preflight.js`): 7 находок, 0
        отброшено. Medium, исправлено до выката: при настоящей гонке двух
        сессий проигравший ждал блокировку шага (`task_step_for_update`) или
        поломки (`for update`), а строка победителя становилась видна только
        после неё — и проверка лимита отвечала `mediaLimitReached` за
        собственный файл (на видео-шаге лимит всегда 1, на четвёртом фото
        поломки, на последнем фото шага). Теперь сразу после блокировки идёт
        повторный поиск по id — та же правка в `add_message_media` через
        помощник `chat_media_written_meanwhile`. Триггерный тест набора этого
        не видит (одна транзакция); проверено настоящей гонкой на видео-шаге:
        сессия B ждала 3,3 с и получила строку сессии A, строка одна. High —
        не код, а рецепт (ниже). Low: заголовки тестов гонки указывали на
        старый номер миграции; добавлен тест «строка чужого автора с тем же id
        отвечает ключом»; откат окна 1 записан ниже.
      **Клиент телефона готов** (2026-09-18): фото читаются вместе с
      сообщениями одним `select` — эмбед `task_media(id, storage_path,
      uploaded_at, created_at)` с фильтрами `deleted_at`/`purged_at` на
      эмбеде (читатель `fetchMessages` уже в `READERS`, сторож проверяет и
      эмбед); плитки строит чистая `messageTiles()` (`features/chat/
      media-tiles.ts`) по строкам и состоянию очереди `useOwnMediaStates()`;
      цепочка `attachMedia` при `message` в переменных зовёт идемпотентный
      `send_message` первым, а на отказ бакета переспрашивает регистрацию,
      чтобы плитка сказала «срок вышел»; черновики фото живут в композере до
      отправки (камера и галерея всегда, лимит `CHAT_MAX_PHOTOS` = 4,
      сжатие как в F8), `send_message(..., media_expected = n)`, на каждое
      фото — мутация в `['media','attach']`; «повторить» только у «не
      загрузилось», «убрать» у «не загрузилось» и «срок вышел».
      Ограничение, принятое сознательно: мутация, упавшая не по сети и
      сброшенная перезапуском до регистрации строки, теряет фото молча —
      отправитель шлёт его заново; сеть в паузу мутацию не роняет.
      **Замер читателей фото (2026-09-18):** на локальной базе с дампом прода
      от 12 сентября под RLS каждого профиля восемь форм читателей (телефон и
      панель, шаги и проблемы, с фильтром `purged_at` и без) дали одинаковые
      числа до и после миграции; один читающий запрос к облаку: из 5 строк
      `task_media` одна вычищена, и она была скрыта от читателей ещё по
      `deleted_at`. Новый фильтр `purged_at is null` не прячет ни одного
      живого фото. Вопрос закрыт.
      **Клиент панели готов** (2026-09-19): тот же эмбед `task_media` с теми же
      фильтрами и в списке `READERS` сторожа; цепочка «регистрация → загрузка →
      подтверждение» в `features/chat/media.ts`; выбор файлов в окне ввода с
      проверкой до отправки (JPEG/WebP, ≤ 20 МБ — зеркала
      `task_media_extension` и `task_media_max_bytes`), иначе сообщение уже
      отправлено, а фото под ним заполнить нечем; плитки строит
      `messageTiles()` (`features/chat/message-tiles.ts`) — зеркало телефонной,
      а не общий модуль: у телефона на входе файлы на диске, у панели объектные
      ссылки браузера. «Повторить» предлагается только пока браузер держит файл
      (`canRetry`): после перезагрузки дырку можно убрать, но не заполнить —
      файл, выбранный в прошлой сессии, браузер не откроет. Источник фото —
      `gallery`: диалог файлов не камера, гейт галереи в чате не применяется,
      но заявление осталось честным. Ключи перевода — под `panel.chat.*`.
      **Предполёт окна 2** (2026-09-19, `wf_0416ca3a-980`,
      `.claude/workflows/chat-window2-preflight.js`, 54 агента, шесть линз):
      16 подтверждённых находок (0 critical, 0 high, 8 medium, 8 low), 0
      отброшено, 42 замера. **Замер новой политики на живом пути — ноль:** на
      20 000 строк `task_media` телефонный `fetchTaskMedia` даёт 164 буфера у
      горничной и 28 у менеджера, бит в бит одинаково до и после сброса
      политики; подзапрос в плане есть, но `never executed`. Закрыто до выката
      (2026-09-19): политика переименована в `"chat message media …"`, чтобы
      `is_manager()` шла в `OR` раньше неё, с проверкой текста `Filter` в
      наборе и с прямым предупреждением в миграции, что порядок
      недокументирован; комментарий к `host_id = current_host_id()` — он держит
      `host_id` в условии индекса `task_media_task_idx`, а не дублирует
      подзапрос; неверное объяснение «первая проверка ложна» заменено
      измеренным; **фото сообщения ждёт обоих сроков** — предмета и своего,
      иначе слово, сказанное в ветке на следующий день после закрытия задачи,
      метётся сразу; тест «строка шага 30 часов без файла не в метёлке» — окно
      чата чужих часов не трогает; просроченная плитка в обоих клиентах.
      **Впереди:** выкат окна 2 по рецепту ниже. Отдельным куском, не в окне 2:
      постраничное чтение ветки — обе `fetchMessages` без `LIMIT`, и стоимость
      политики на фото растёт с длиной разговора.
- [ ] 6. Личная ветка (вместе с F11)
- [ ] 7. Летопись: системные строки о событиях предмета, связка уборки и
      поломки, переключатель «это инструкция». Запрос владельца 2026-09-18:
      чат — ещё и хроника уборки, «начата в 10:12», «завершена в 11:40»,
      «отменена», не в виде пузыря, а строкой-меткой между сообщениями.
      Хранить, а не вычислять: у задания нет времени и автора отмены, взятие и
      переназначение не остаются нигде, и хроника, собранная на экране из
      колонок строки, врала бы после каждой правки. Форма: `chat_messages.kind`
      (`text` / `event`) плюс `event` jsonb (`{"type": "started", "at": …,
      "by": …}`), пишет триггер на смене статуса задачи и поломки в ветку
      предмета (через ту же подмену, что `open_thread`); `author_role` у
      события — роль того, кто действовал. Текст события клиенты собирают из
      словаря по `type`, сервер текста не хранит (правило `CLAUDE.md`).
      **Непрочитанное не считает события за сообщения** (слой 4 вышел
      раньше): `chat_unread_threads` сравнивает `last_message_at` с маркером,
      поэтому событие не должно двигать хвост нити — триггер
      `chat_touch_thread` при `kind = 'event'` хвост не трогает, а
      `message_count` считает только текст; иначе «уборка завершена» зажжёт
      метку у менеджера, который её и завершил.

## Эксплуатация выката

- **Окно 1 выкачено 2026-09-19, около 01:05.** Применена одна миграция
  `20260918170000_media_rpc_race.sql`: сухой прогон назвал ровно её, три файла
  окна 2 на время `db push` лежали вне каталога и вернулись побайтово
  (`sha256sum -c` — три `OK`, дерево чистое). Гранты сверены в облаке сразу
  после выката: у `task_media` `authenticated` держит только `SELECT`, `anon` —
  ничего (щедрых default privileges хостинг не подсыпал: таблица не
  пересоздавалась); `add_task_media` и `add_problem_media` остались
  `security definer` с `EXECUTE` у `authenticated` и `service_role`, у `anon`
  его нет; перегрузок не появилось — по одной сигнатуре на имя, то есть
  `create or replace` попал в прежнее тело, а не завёл второе. Остаётся день
  наблюдения; окно 2 — после панели.
- **Порядок выката окна 2: `db push` → OTA телефона → Vercel.** Панель едет
  последней, и это не вкусовщина. Стоит ей приехать раньше телефонов, менеджер
  сможет отправить сообщение из одного фото без слов, а сборка, которая стоит
  в поле, рисует только `body` — горничная получит пузырь с автором, временем
  и пустотой, и не на минуты, а на дни, пока расходится OTA. Новой сборке
  телефона нужны только миграции, а они и так первые.
- **Выигрыш от порядка политик держится на недокументированном поведении.**
  Пермиссивные политики складываются в один `OR`, и Postgres выстраивает ветки
  по именам политик **в порядке убывания**; на этом стоит решение назвать
  чатовую политику так, чтобы `is_manager()` шла раньше (замер: 2383 буфера
  против 983 на ветке из 20 сообщений с 40 фото, 37 929 против 11 049 на 220
  сообщениях с 840 фото). Контракта на этот порядок нет: после апгрейда
  Postgres он может исчезнуть молча — чтения останутся правильными, только
  подорожают, и ни один тест не покраснеет. Перемер записан в `F13. Hardening`
  (`docs/ROADMAP.md`). Смотреть так, на пустой таблице:
  ```
  begin;
  set local role authenticated;
  set local request.jwt.claims = '{"sub":"<uuid>","role":"authenticated"}';
  explain (costs off) select id from public.task_media where task_id = '<uuid>';
  rollback;
  ```
  В `Filter` ветка `is_manager()` обязана стоять раньше ветки с `message_id`.
- **Предполётная проверка перед каждым схемным этапом** (1, 4, 5) — как перед
  выкатом юнитов: состязательный прогон на копии боевых данных, отвечающий на
  три вопроса. Сколько живых строк нарушат новое ограничение
  (`task_media_one_owner` на этапе 5 — ожидание ноль). Сколько веток и
  сообщений увидит менеджер самой большой компании и сколько это стоит виду
  `chat_unread` по `explain analyze`. Не удлиняет ли новая политика на
  `task_media` чтение медиа шага, которое идёт на каждом экране задачи.
- **Окно 1 выкатывается одним файлом, а CLI такого флага не знает.**
  `supabase db push` применяет все локальные миграции новее удалённой головы
  (в облаке — `20260918160000`), флага «до версии» у `db push` и
  `migration up` нет (CLI 2.115, находка предполёта). Три файла окна 2 на
  время выката убираются из каталога и возвращаются после:
  ```
  New-Item -ItemType Directory -Force ..\window2-parked
  Move-Item supabase\migrations\20260918180000_chat_media.sql ..\window2-parked\
  Move-Item supabase\migrations\20260918190000_chat_rpc_race.sql ..\window2-parked\
  Move-Item supabase\migrations\20260918200000_chat_media_expiry.sql ..\window2-parked\
  npx supabase db push --dry-run      # должен назвать только 20260918170000
  npx supabase db push
  Move-Item ..\window2-parked\*.sql supabase\migrations\
  git status                          # дерево должно быть чистым
  ```
  Крон гасить не нужно: миграция заменяет две функции, которые ни одна
  задача `cron.job` не вызывает; старые бандлы телефона и панели зовут те же
  сигнатуры и видят одно отличие — повтор, прежде падавший с `23505`, теперь
  возвращает строку (на `23505` в клиентах никто не опирается).
  **Откат окна 1**, если понадобится: одной транзакцией как `postgres` —
  тела `add_task_media` и `add_problem_media` из
  `20260917110100_task_media_source.sql` (`create or replace` сохраняет
  гранты), затем `drop function public.task_media_written_meanwhile(uuid, uuid, uuid)`;
  ничего записанное за день старые тела не ломает. Строка истории
  `20260918170000` остаётся «применена»: исправление везётся новой, более
  поздней миграцией.
- **Рецепт выката окна 2** (подготовлен 2026-09-19, не выкачен). Груз — три
  файла: `20260918180000_chat_media`, `20260918190000_chat_rpc_race`,
  `20260918200000_chat_media_expiry` (номера сменятся на шаге 0). Убирать из
  каталога нечего, они и есть весь остаток.
  0. **Свежие номера — в момент собственного выката** (решение 2026-09-23).
     Пока окно 2 ждёт, в облако уезжают миграции новее его файлов (первой —
     `20260923120000`), и три файла оказываются позади удалённой головы. CLI
     2.115 такой push отвергает («Found local migration files to be inserted
     before the last migration on remote database») и предлагает
     `--include-all`. **`--include-all` не берём:** он применит окно 2 в
     облаке ПОСЛЕ всего, что уже там, а локальный `db:reset` по-прежнему
     применяет файлы по имени — ДО. Порядок в облаке и на локальном стеке
     разойдётся, и зелёные локальные тесты перестанут отвечать за облако.
     Переименовывать заранее тоже бессмысленно: следующая миграция снова
     поставит окно 2 позади. Поэтому номера меняются ровно тогда, когда окно
     2 едет само: в облаке этих файлов нет, переименование бесплатно. Шаги:
     - три файла — на номера новее текущей головы облака, с сохранением их
       взаимного порядка (`chat_media` → `chat_rpc_race` →
       `chat_media_expiry`);
     - те же номера — во всех ссылках на них: заголовки самих трёх файлов
       (ссылаются друг на друга), `supabase/tests/chat.sql`,
       `.claude/workflows/chat-window2-preflight.js`, этот документ. Упоминания
       в уже выкаченных миграциях (`20260918170000_media_rpc_race.sql`) и в
       старых предполётах — история, их не трогать;
     - полный цикл с `db:reset`: теперь локальный стек применяет окно 2 после
       генератора, как это сделает облако.
  1. **Предполёт заново.** Все три файла изменились после его прогона
     (`wf_0416ca3a-980`), так что кэш прошлого прогона на них не отвечает:
     `Workflow({scriptPath: '.claude/workflows/chat-window2-preflight.js'})`.
  2. Застава и push одной командой — по правилу из `docs/units-plan.md`,
     «Эксплуатация выката»: голова облака (сейчас уже не `20260918170000`)
     и сухой прогон, который называет ровно три переименованных файла.
  3. **Не в окно 04:30.** Миграция заменяет `task_media_to_purge`, а её зовёт
     Edge Function `purge-task-media` по крону. `create or replace` дождётся
     конца прогона, но выкат посреди метёлки оставит остаток ночи на прежнем
     теле. Мести в окне 2 всё равно нечего — фото сообщений в облаке нет, — так
     что достаточно не совпасть по времени; гасить крон (рецепт в
     `docs/units-plan.md`) нужно только если выкат попадает в окно.
  4. **Гранты в облаке сразу после push.**
     `information_schema.role_table_grants` для `anon` и `authenticated` по
     `task_media`: у `authenticated` ожидается только `SELECT`, у `anon` ни
     строки. Новая колонка прав не меняет, но хостинг щедрее локального стека.
     Затем `has_function_privilege` на `add_message_media` и
     `chat_media_upload_window` (`authenticated` — да) и на
     `task_media_to_purge` (`authenticated` — нет), и по одной сигнатуре на
     имя: перегрузка значит, что `create or replace` завёл вторую функцию
     вместо замены тела.
  5. **OTA телефона, и только потом панель на Vercel** — порядок объяснён выше.
     Перед `eas update` ещё раз `migration list --linked`: если пуш не прошёл,
     телефон попросит схему, которой в облаке нет.
  **Обратной стороны у этого выката нет.** `20260918180000` добавляет колонку
  `task_media.message_id` и переписывает `task_media_one_owner`; снять колонку
  значит увести и строки фото сообщений. Чинить — новой миграцией вперёд, как
  окно 1 починило гонку. Пока не отправлено ни одного фото сообщения, обратный
  шаг сводится к `drop policy` и возврату трёх тел функций из предыдущих
  миграций, но этот путь не проверен — проверять его придётся до того, как он
  понадобится, а не в тот момент.
- **Крон гасится на время `db push`** — рецепт в `docs/units-plan.md`,
  «Эксплуатация выката». Прямая правка `cron.job` в облаке запрещена:
  `permission denied for table job`.
- **Гранты сверяются в облаке после каждого `db push`** —
  `information_schema.role_table_grants` для `anon` и `authenticated`: хостинг
  раздаёт новым таблицам default privileges щедрее локального стека.
- **`db:types` — только сразу после `db:reset`.** Файл снимается с базы, а не с
  миграций, и на грязной базе повторяет то, чего в миграциях нет.

## За рамками F29

Переписка менеджер↔менеджер и горничная↔горничная; групповые ветки; вложения,
кроме фото; правка и удаление отправленного (сказанное назад не забирают —
снять можно только застрявшую загрузку); поиск по переписке; шаблоны ответов;
внешние каналы — WhatsApp остаётся вне объёма решением 2026-09-04, доставку
даёт push (F11).

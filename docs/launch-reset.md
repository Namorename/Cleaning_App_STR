# Обнуление перед запуском — проект

**Статус: только проект, ничего не выполнено** (2026-09-23). Решение владельца:
перед запуском всё прошлое обнуляется целиком — задачи с датой до дня запуска и
всё, что на них держится. Накопленные дубли `expired` (3 890 по замеру
2026-09-23) уходят вместе с ним, отдельной чистки не будет, уникальный индекс
не ставится (`docs/f10-plan.md`, «Решения владельца», 2).

Ниже **L** — день запуска: первый день, когда горничные работают в приложении
по-настоящему. Все даты — по Праге (108 объектов `Europe/Prague`, 2
`Europe/Berlin`, пояса совпадают).

Числа «сегодня» — проба `docs/rollout/prelaunch_probe.sql` (облако,
2026-09-23, только счётчики). К дню L они вырастут; сухой прогон (шаг 1)
перемеряет их на L+1.

## 1. Что уходит и что остаётся

| Что | Сейчас | Решение | Почему |
| --- | --- | --- | --- |
| Задачи с `scheduled_date < L` | 5 074 (4 990 `expired`, 47 `cancelled`, 16 `done`, 21 живая; 6 ремонтов) | удалить | решение владельца |
| `task_steps` | 37, все на прошлых задачах | уходят каскадом | `task_steps.task_id → cascade` (`20260905110300:20`) |
| Строки `task_media` задач | 5 | уходят каскадом, **но файлы — раньше строк** (§3) | `task_media.task_id`, `step_id → cascade` (`20260907160100:95-96`) |
| Файлы этих строк в бакете `task-media` | ≤ 5 из 9 (1,4 МБ на весь бакет) | удалить через ретеншн до удаления строк | удаление строки файл не удаляет: у `storage.objects` нет FK на `task_media`, ретеншн ходит по строкам, а не по бакету, и файл навсегда остаётся сиротой, недоступной клиенту |
| Чаты с предметом-задачей и их сообщения | 7 веток задач, все на прошлых задачах; 20 сообщений на весь чат | уходят каскадом вместе с `chat_reads` | `chat_threads.task_id → cascade`, `chat_messages.thread_id → cascade` (`20260918120000:53, 90, 120`). Ремонт пишет в ветку поломки, не задачи |
| Поломки | 6 (5 `open`, 1 `resolved`), 2 заведены из задачи, все — тестовые до запуска (тестовая `c8ff0001-…`) | предложение: удалить все, заведённые до L, вместе с их ремонтами, фото и ветками; настоящую открытую поломку, если к L такая будет, — оставить (вопрос 2) | `problems.task_id → set null`: без решения поломки пережили бы задачи с пустой ссылкой |
| Ремонты (задачи с `problem_id`) удаляемых поломок | 6, все `cancelled`/`done` | удалить явно, до поломок | иначе `tasks.problem_id → set null`, и задача без поломки попадает под метёлку и в «Задания» как ручная |
| Фото поломок (`task_media.problem_id`) | 5 | файлы — через ретеншн, строки — каскадом с поломкой | `20260908130100:13`, путь `host/problems/<id>/…` |
| Ветки чата поломок | 5 | каскадом с поломкой | `chat_threads.problem_id → cascade` |
| Заявки на расходники | 3 `fulfilled`, тестовые (Мария) | предложение: удалить (вопрос 3) — иначе они держат удаление тестовой учётки | `supply_requests.task_id → set null`, `requested_by → restrict` (`20260908130200:39, 42`) |
| Брони, `reservation_units`, `raw.hostaway_reservations` | 2 452 / 640 / 2 455 | **не удаляем** | зеркало Hostaway: ночная сверка и вебхук вернут часть сами, а `raw.hostaway_reservations` — источник переигровки нормализации |
| `raw.generator_runs` | 13 | оставить | только счётчики и даты, чистится сам через 90 дней; единственное доказательство «до и после» (§4, шаг 5) |
| `raw.webhook_events` | 24 221 (27.08–23.09), в `payload` имена и телефоны гостей; ретеншна нет | предложение: удалить `processed` и `skipped`, полученные до L, в том же окне; `pending` и `failed` оставить (вопрос 4) | обработчик читает только `pending`, полезная нагрузка ему не нужна (`20260827200000:111-131`); `failed` — единственный след ошибок загрузки брони |
| Объекты, чек-листы, процессы, привязки горничных | — | оставить | это настройка, не история. Но привязки тестовых горничных — см. учётки |
| Тестовый шаг `photos_after` в стандартном шаблоне `0f0951a9-…` | вставлен прямым insert в облаке при проверке F8 | решить (вопрос 6) | артефакт проверки внутри боевой настройки |

**Учётки.** В облаке 5 пользователей: горничные — 2 активные и 1 отключённая,
менеджеры — 2 активных; на `@example.com` — три: `maria.test`, `anna.test`,
`manager.test`. Пароли тестовых учёток упрощены (заметки в памяти и файлах
сессий), и прежнее правило владельца — «перед боевым запуском вернуть
сложные».

- **Привязки тестовых горничных снимаются в любом случае, и до L.** По записи
  сессии 2026-09-01 у Марии и Анны `auto` на листингах `412432` и `363105`
  плюс `claim` на `219524/219525` (сверить в сухом прогоне; ROADMAP на 19.09
  насчитал два `auto` из 19 связей). С `auto` генератор автоназначит им
  настоящие уборки дня запуска.
- **Удаление** — через Admin API (в `manage-staff` удаления нет) и только после
  того, как ушли их поломки и заявки (`reported_by`, `requested_by` —
  `restrict`) и у них нет живых назначенных задач (иначе CHECK
  `tasks_assigned_has_assignee`). Каскадом уходят их привязки, личные ветки
  чата и отметки прочтения; в задачах, шагах и медиа их id становится `null`,
  подпись сообщения остаётся в `author_name`.
- **Отключение** (`manage-staff update isActive=false`) держит все ссылки и
  закрывает вход через `is_active_user()`. `manager.test` отключается только
  чужой менеджерской учёткой — себя менеджер не отключает.
- Предложение и выбор — вопрос 5.

## 2. Когда стирать: генератор и вчерашние уборки

Таблица `tasks` — память генератора о том, какие брони уже обслужены. Удалённая
уборка делает бронь снова «должной», и генератор рождает её заново, если день
не просрочен: `task_is_stale` = дата раньше `сегодня − 1` (отсрочка —
`task_grace_days() = 1`, `20260904120100:28-59`; граница рождения —
`20260923120000:277-331`).

- **Стирать в день L нельзя.** Вчерашний день L−1 ещё в отсрочке. Первый же
  вызов генератора, окно которого его накрывает, вернёт все уборки L−1: ночной
  прогон в 03:15 UTC (окно сегодня−7…+90) или любая пачка вебхука (каждые две
  минуты, 124–179 пачек в сутки). В 03:30 UTC на L+1 метёлка закроет их как
  `expired`, и в чистой истории навсегда останутся строки L−1. А сами уборки L−1
  в день L ещё могут делаться.
- **Стирать на L+1, после ночной метёлки.** С 00:00 по Праге L−1 уже просрочен,
  и граница рождения его не пропустит. После `expire-stale-tasks` (30 3 UTC)
  все уборки до L — конечные, ни один проход генератора их не пишет. Исключение —
  ремонты: метёлка их не трогает, поэтому они удаляются явно. Момент — после
  того, как `cron.job_run_details` покажет успех метёлки, например 04:00 UTC
  (06:00 по Праге летом, 05:00 зимой) или позже, когда владелец на месте. Мимо
  окон 03:10–03:35 и 04:25–04:40 UTC: в 04:30 идёт `purge-task-media-daily` по
  тем же строкам и файлам.
- **Крон не гасить.** На L+1 рождать нечего, а `unschedule` удаляет задание,
  и вернуть его можно только полным определением (`docs/units-plan.md`,
  «Эксплуатация выката»). Забытое возвращение молча остановит вебхуки.
- **Если день L должен начаться без истории**, то в два прохода, оба после
  метёлки: на L — всё `≤ L−2` (уже просрочено), на L+1 — `L−1`.
- **Ещё один класс для сухого прогона:** брони с выездом `≥ L`, которых сейчас
  «обслуживает» только строка раньше L (`done`/`in_progress` на старой дате
  брони, сдвинутой после уборки). После стирания они получат новую уборку на
  новую дату — это правильно, но число надо знать заранее.

## 3. Файлы: сначала файл, потом строка

Клиент объекты не удаляет, удаляет только ретеншн (CLAUDE.md, «Медиа уборки»).
Им и пользуемся. Ветка `deleted_at is not null` в `task_media_to_purge` делает
строку «должной» сразу, без условий, во всех версиях функции.

1. `update public.task_media set deleted_at = now()` для строк удаляемых задач и
   удаляемых поломок, где `deleted_at is null`.
2. Позвать `select public.invoke_edge_function('purge-task-media')`. Функция
   удаляет файлы через Storage API сервисной ролью, по 200 строк, до 2 000 за
   прогон, и ставит `purged_at`.
3. Проверить: у всех строк набора `purged_at` есть, в `storage.objects` нет ни
   одного объекта по их `storage_path`.
4. Только после этого удалять строки.

Если окно 2 к тому моменту в облаке, в набор входят и фото сообщений на ветках
удаляемых задач и поломок (`task_media.message_id → chat_messages cascade`,
путь `host/chat/<thread>/…`): иначе каскад оставит их файлы сиротами.

## 4. Порядок

1. **Сухой прогон с числами** — читающая проба (только счётчики, без
   персональных полей), на L+1 после метёлки, до копии:
   - задачи `< L` по статусу и типу, ремонты среди них;
   - `task_steps`, строки `task_media` по владельцу и состоянию, объекты
     бакета по их путям;
   - ветки и сообщения чата на этих задачах и поломках;
   - поломки и заявки, в том числе заведённые тестовыми учётками;
   - живые назначенные задачи и привязки тестовых учёток;
   - класс «бронь `≥ L`, обслуженная строкой `< L`»;
   - события вебхука к удалению по статусу;
   - прогноз ночного `past_bound`: брони с выездом L−5…L−1 в разрезе
     `_wanted` (по комнате из `reservation_units`, иначе по листингу; статусы
     `new`/`modified`, не блок, объект активен).

   Основа — `docs/rollout/prelaunch_probe.sql`, к нему добавляются метки для
   L.
2. **Копия, не таская имена гостей на локальную машину.**
   - **Архивная схема в облачной базе.** `create table archive_prelaunch.<имя>
     as select …` под `postgres` для удаляемых строк: `tasks`, `task_steps`,
     `task_media`, `chat_threads`, `chat_messages`, `chat_reads`, `problems`,
     `supply_requests` с позициями и удаляемые `raw.webhook_events`. Данные не
     покидают сервер. Схема закрыта как `raw`: `revoke` у `anon`,
     `authenticated` и `public` — на схему и на каждую таблицу поимённо, затем
     проверка `information_schema.role_table_grants` в облаке. У схемы
     записанная дата удаления (например, L+30). CTAS не копирует ограничения,
     индексы и гранты — для отката строки вставляются обратно в порядке
     внешних ключей.
   - **Файлы** (≤ 9, 1,4 МБ, фото квартир без имён гостей) — копия Storage API
     `copy` в приватный бакет без политик или сознательный отказ
     (вопрос 7).
   - **Платформенный бэкап** — убедиться в Dashboard → Database → Backups, что
     после сухого прогона есть свежий. Это страховка от катастрофы, а не
     выборочный откат: восстановление откатывает весь проект, файлов бакета в
     нём нет.
   - **Локальный дамп — нет.** Он потащил бы имена гостей (`raw.*`,
     `reservations.guest_name`); прецедент 12.09 — 269 МБ сырых броней на
     локальной машине, удалены после.
3. **Слово владельца** — по числам сухого прогона и проверке копии.
4. **Выполнение**, одним скриптом в окне §2:
   1. §3: файлы задач и поломок через ретеншн, проверка;
   2. `begin`;
   3. ремонты удаляемых поломок;
   4. поломки (каскад: их фото, уже без файлов, и ветки чата);
   5. задачи `scheduled_date < L` (каскад: шаги, медиа, ветки задач);
   6. заявки тестовых учёток, если решено;
   7. события вебхука, если решено;
   8. `commit`;
   9. привязки тестовых учёток — снять, сами учётки — по решению 5, через Admin
      API.
5. **Проверка после** — счётчики:
   - задач `< L` — 0;
   - `storage_objects_without_row` — 0 (сирот нет);
   - строк `task_media` без файла, не отмеченных `purged_at`, — 0;
   - архивная схема закрыта для клиентов, `role_table_grants` для `anon` и
     `authenticated` не сдвинулись;
   - первый ночной `raw.generator_runs` после стирания: строка есть,
     `past_bound` ≈ прогнозу (ночью L+2 он считает выезды L−5…L−1, дальше
     убывает на день каждую ночь);
   - инвариант границы из `docs/units-plan.md` (шаг 8 рецепта пакета) с
     `created_at >= <момент стирания>` — 0;
   - задач с `scheduled_date < L` и `created_at >= <момент стирания>` — 0.

   Пачки вебхука, чей диапазон захватывает старые выезды, покажут большой
   `past_bound` — это «отказано в рождении», не дефект.

## Вопросы к владельцу

1. **День L** и кто на месте на L+1 после 04:00 UTC для слова и проверки.
2. **Поломки:** удалить все шесть заведённых до L (все тестовые) — или, если к L
   появится настоящая открытая, её оставить?
3. **Заявки на расходники** (3, тестовые): удалить вместе с обнулением?
4. **`raw.webhook_events`:** удалить `processed` и `skipped` до L в том же окне?
   И нужен ли после запуска ретеншн, например 30 дней, отдельной миграцией (F24,
   GDPR)? Сейчас там 24 тысячи событий с именами и телефонами, и они только
   копятся.
5. **Тестовые учётки:** Марию и Анну — удалить после обнуления, `manager.test` —
   удалить или оставить со сложным паролем и отключить? Кто третья горничная
   (отключённая) и чья вторая менеджерская учётка, счётчики не говорят.
6. **Тестовый шаг `photos_after`** в стандартном шаблоне `0f0951a9-…` — убрать?
7. **Копия:** архивная схема в облаке как разовая операция, записанная здесь с
   датой удаления, или отдельной миграцией (правило «схема — только
   миграциями»)? Файлы — копировать в приватный бакет или отказаться (фото
   тестовых уборок)?
8. **Окно 2** к дню L будет в облаке? От этого зависит, входят ли фото
   сообщений в §3.
